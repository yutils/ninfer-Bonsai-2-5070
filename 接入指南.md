# 接入指南 · 把本地 NInfer 接到 opencode 等工具

服务是 **OpenAI 兼容 + Anthropic 兼容** 双协议。绝大多数工具只要填一个 Base URL 就能用。

**前提**：先双击 `start-server.bat`，等服务窗口出现 `listening on http://127.0.0.1:8080`。

| 项目 | 值 |
|---|---|
| Base URL（OpenAI 协议） | `http://127.0.0.1:8080/v1` |
| Base URL（Anthropic 协议） | `http://127.0.0.1:8080` |
| Model ID | `bonsai2-27b`（`start-server.bat` 里的 `MODELID`，改了要同步） |
| API Key | 任意字符串，例如 `local`（服务没开鉴权，但很多工具要求非空） |
| 上下文 | 32768（`/v1/models` 的 `max_model_len`） |

---

## 1. opencode

配置文件：`~/.config/opencode/config.json`（Windows 为 `%USERPROFILE%\.config\opencode\config.json`）。

**新版 schema（推荐）：**

```json
{
  "$schema": "https://opencode.ai/config.json",
  "provider": {
    "ninfer": {
      "npm": "@ai-sdk/openai-compatible",
      "name": "NInfer (local)",
      "options": {
        "baseURL": "http://127.0.0.1:8080/v1",
        "apiKey": "local"
      },
      "models": {
        "bonsai2-27b": {
          "name": "Ternary Bonsai 2 27B"
        }
      }
    }
  },
  "model": "ninfer/bonsai2-27b"
}
```

**旧版 schema（`providers` 复数）：**

```json
{
  "$schema": "https://opencode.ai/config.json",
  "providers": {
    "ninfer": {
      "npm": "@ai-sdk/openai-compatible",
      "name": "NInfer (local)",
      "options": {
        "baseURL": "http://127.0.0.1:8080/v1",
        "apiKey": "local"
      },
      "models": {
        "bonsai2-27b": { "name": "Ternary Bonsai 2 27B" }
      }
    }
  },
  "model": "ninfer/bonsai2-27b"
}
```

也可用环境变量：`OPENAI_BASE_URL=http://127.0.0.1:8080/v1`、`OPENAI_API_KEY=local`。

---

## 2. Claude Code（走 Anthropic 协议）

```powershell
setx ANTHROPIC_BASE_URL http://127.0.0.1:8080
setx ANTHROPIC_AUTH_TOKEN local
setx ANTHROPIC_MODEL bonsai2-27b
```

重开终端后 `claude` 即可。服务端点是 `/v1/messages`。

---

## 3. Continue（VS Code / JetBrains）

`~/.continue/config.json`：

```json
{
  "models": [
    {
      "title": "Bonsai 2 27B (local)",
      "provider": "openai",
      "model": "bonsai2-27b",
      "apiBase": "http://127.0.0.1:8080/v1",
      "apiKey": "local",
      "contextLength": 32768
    }
  ]
}
```

---

## 4. Cline / Roo Code

在设置里选 **OpenAI Compatible**：

- Base URL: `http://127.0.0.1:8080/v1`
- API Key: `local`
- Model ID: `bonsai2-27b`
- Context Window: `32768`

---

## 5. Cherry Studio / NextChat / 其它 OpenAI 前端

新建「OpenAI」类型渠道：

- API 地址 `http://127.0.0.1:8080`
- 密钥 `local`
- 模型手动填 `bonsai2-27b`（点「获取模型列表」也能拿到）

---

## 6. OpenAI SDK（Python）

```python
from openai import OpenAI

client = OpenAI(base_url="http://127.0.0.1:8080/v1", api_key="local")

r = client.chat.completions.create(
    model="bonsai2-27b",
    messages=[{"role": "user", "content": "用一句话解释哈达玛变换"}],
    max_tokens=512,
)
print(r.choices[0].message.content)
print(r.usage)  # prompt/completion/total tokens
```

> 思维链在 `message.reasoning_content`，不在 `content`。

---

## 7. curl 冒烟测试

```powershell
curl.exe http://127.0.0.1:8080/v1/models

curl.exe -N http://127.0.0.1:8080/v1/chat/completions ^
  -H "Content-Type: application/json" ^
  -d "{\"model\":\"bonsai2-27b\",\"messages\":[{\"role\":\"user\",\"content\":\"1+1\"}],\"stream\":true}"
```

---

## 排错

| 现象 | 处理 |
|---|---|
| `404 model_not_found` | `"model"` 必须是 `/v1/models` 里返回的 id。先 `curl http://127.0.0.1:8080/v1/models` 确认 |
| 连不上 / `ECONNREFUSED` | 服务没起。看 `start-server.bat` 窗口有没有 `listening on ...` |
| 请求发出但无回音 | 客户端走了系统代理。给 localhost 配 `no_proxy=127.0.0.1,localhost` |
| 工具要求 max_tokens > 32768 | 本机上限见 README；要么调小请求，要么把 `CTX` 调大（显存允许的话） |
| 工具显示「不支持 function calling」 | 服务端支持 tool calls，但需在工具里显式开启；必要时加 `--tolerant-tool-calls` |
| 回答里混着大段思考 | 那是 `reasoning_content`。服务端用 `--no-thinking` 可全局关闭 |

### 工具要求更长的上下文

`start-server.bat` 里 `CTX` 最大可用 **65536**（fp8 KV）。
想再大：把 `KVTYPE` 改成 `int8` / `nvfp4` / `k8v4`，再往上调 `CTX`。

改完**必须重启服务**，并同步更新客户端里填的 context length。
